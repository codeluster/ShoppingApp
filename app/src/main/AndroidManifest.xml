<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.tanmay.shoppingapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/Theme.Tanmay">
        <activity android:name=".CatalogActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ProductPage"
            android:label="@string/title_activity_product_page"
            android:parentActivityName=".CatalogActivity"
            android:theme="@style/Theme.Tanmay">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.example.tanmay.shoppingapp.CatalogActivity" />
        </activity>
        <activity
            android:name=".YourCart"
            android:label="@string/title_activity_your_cart"
            android:parentActivityName=".CatalogActivity"
            android:theme="@style/Theme.Tanmay">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.example.tanmay.shoppingapp.CatalogActivity" />
        </activity>

        <provider
            android:name=".Data.Cart.DataProvider"
            android:authorities="com.example.tanmay.shoppingapp"
            android:exported="false" />

        <activity android:name=".SignUp" />
        <activity android:name=".UserAccount"></activity>
    </application>

</manifest>